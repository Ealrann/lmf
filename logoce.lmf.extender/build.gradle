plugins {
	id 'maven-publish'
}

publishing {
	publications {
		maven(MavenPublication) {
			groupId = 'org.logoce.lmf'
			artifactId = 'logoce.lmf.extender'
			version = "$logoceVersion"

			from components.java
		}
	}
}
